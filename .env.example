# Firebase web app
NEXT_PUBLIC_FIREBASE_API_KEY=""
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=""
NEXT_PUBLIC_FIREBASE_PROJECT_ID=""
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=""
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=""
NEXT_PUBLIC_FIREBASE_APP_ID=""

# Mercado Pago - preencher quando SPEC-PAYMENTS-MP for ligada (sandbox/prod).
# Sem estas variaveis, o checkout roda em "modo simulado" (nao quebra o build).
MP_ACCESS_TOKEN=""
MP_WEBHOOK_SECRET=""
MP_PUBLIC_KEY=""
NEXT_PUBLIC_MP_PUBLIC_KEY=""
MP_WEBHOOK_URL=""
MP_SITE_URL=""
MP_ENVIRONMENT="sandbox"

# Firebase Admin SDK (server-only) - usado pelas rotas /api/** e pelos scripts de seed.
# Forneca UMA das duas formas (nunca commitar o valor real):
#   - FIREBASE_SERVICE_ACCOUNT_BASE64: o JSON da service account em base64
#   - GOOGLE_APPLICATION_CREDENTIALS: caminho para o arquivo serviceAccount.json
FIREBASE_SERVICE_ACCOUNT_BASE64=""
# FIREBASE_SERVICE_ACCOUNT_KEY=""
# GOOGLE_APPLICATION_CREDENTIALS=""

# Banco Firestore: vazio = (default). Definir o ID nomeado SO apos o PREFLIGHT
# (decisao #11) confirmar onde as colecoes/rules vivem.
FIREBASE_DATABASE_ID=""
